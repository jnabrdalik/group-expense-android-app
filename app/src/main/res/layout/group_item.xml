<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="group"
            type="com.example.groupexpenseapp.db.entity.Group" />

        <variable
            name="expenseSum"
            type="Double" />

        <variable
            name="personCount"
            type="Integer" />

        <variable
            name="clickListener"
            type="android.view.View.OnClickListener" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/group_card_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{clickListener}"
        android:orientation="horizontal"
        app:cardCornerRadius="4dp"
        app:cardElevation="4dp"
        android:layout_marginHorizontal="@dimen/std_padding"
        android:layout_marginTop="@dimen/std_padding"
        app:contentPadding="@dimen/std_padding">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <TextView
                android:id="@+id/group_name"
                android:textAppearance="?attr/textAppearanceHeadline4"
                android:textColor="?android:attr/textColorPrimary"
                tools:text="Impreza"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{group.name}"
                android:layout_marginBottom="8dp" />
            <TextView
                android:id="@+id/group_person_count"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text="8 osób"
                android:textAppearance="?attr/textAppearanceCaption"
                android:text="@{@string/person_count(personCount)}" />
            <TextView
                android:id="@+id/group_expense_sum"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text="128,83 zł łącznie"
                android:textAppearance="?attr/textAppearanceCaption"
                android:text="@{@string/total_spent(expenseSum)}" />
            <TextView
                android:id="@+id/group_creation_time"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text="Utworzono 25.05.2020"
                android:textAlignment="textEnd"
                android:textAppearance="?attr/textAppearanceCaption"
                android:text="@{@string/group_created(group.formattedTimeCreated)}" />
        </LinearLayout>


    </com.google.android.material.card.MaterialCardView>
</layout>